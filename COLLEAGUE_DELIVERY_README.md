# ailunwen 同事交付说明

## 包含内容

- 最新前后端源代码与管理端代码
- `frontend/dist` 最新生产构建
- `package.json`、`package-lock.json` 与 Python 依赖清单
- 项目文档、部署脚本和配置示例
- 2026-08-05 完成的管理接口鉴权及伪造用户身份防护修复
- 2026-08-06 完成的登录页提示文案调整

## 安全排除项

本交付包不包含以下内容：

- `.env`、API Key、JWT 密钥、数据库密码等真实凭据
- SQLite/PostgreSQL 数据、材料知识库及 SQL 备份
- `.venv`、`.venv-mineru`、`node_modules` 等本机环境或依赖缓存
- `.cursor`、缓存、日志、临时文件和旧构建目录

数据库和生产环境密钥必须通过经授权的安全渠道单独提供，不要写入代码仓库或聊天记录。

## 本地启动

1. 安装项目要求的 Node.js 版本（生产部署当前使用独立 Node 22）。
2. 在项目根目录运行 `npm ci`。
3. 依据文档创建自己的 `.env`；不要覆盖或索取生产服务器的 `.env`。
4. 开发环境按 `package.json` 中的脚本启动。

## 生产状态说明

- 当前生产域名：`https://syncsee.cstdata.net/`
- 后端生产服务仅监听服务器回环地址 `127.0.0.1:8877`
- 生产数据库、服务配置和密钥不在本交付包中

交付日期：2026-08-06
