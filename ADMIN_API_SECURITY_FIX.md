# 管理接口鉴权安全修复说明

## 核心安全问题

修复前，在未登录、未携带认证令牌的情况下访问：

```text
/api/v1/admin/users
```

接口仍会返回 HTTP `200` 和用户记录，导致管理接口及用户数据存在未授权访问风险。

## 具体问题

`/api/v1/admin/users` 是用于查看系统用户列表的管理员接口。正常情况下，只有已经登录并且具有管理员权限的用户才能访问它。

“未登录、未携带令牌”表示访问者没有通过账号密码登录，也没有在请求中提供能够证明身份的 JWT。修复前，服务器没有正确拦截这类请求，仍返回 HTTP `200`，即把请求当作成功请求处理，并可能返回用户名、邮箱、手机号等用户记录字段，实际暴露内容取决于接口返回的数据。

这属于访问控制缺失或未授权访问。攻击者不需要获得管理员密码，只要知道接口地址，就可能直接读取本应仅供管理员查看的数据。

相关 HTTP 状态码如下：

- `200 OK`：请求成功，服务器正常返回数据。
- `401 Unauthorized`：尚未登录，或者令牌缺失、无效、过期。
- `403 Forbidden`：已经登录，但当前用户不是管理员，没有访问权限。

修复后的正确行为是：

```text
未登录或令牌无效 → 401
普通用户登录后访问 → 403
授权管理员访问 → 200
```

修复后的管理接口访问流程是：

```text
访问 /api/v1/admin/*
→ 读取 Authorization: Bearer <JWT>
→ 验证 JWT 是否真实、有效且未过期
→ 从令牌中取得服务端认可的用户身份
→ 检查用户名是否位于 ADMIN_USERNAMES
→ 仅允许管理员继续访问
```

该修复同时阻止客户端通过伪造 `x-user-id` 请求头或请求体中的 `userId` 冒充其他用户。服务器现在只信任经过 JWT 验证得到的身份，不再直接采用客户端自行声明的用户编号。

## 主要修改文件

### `backend/index.js`

- 给所有 `/api/v1/admin/*` 接口统一增加 `requireAdmin` 鉴权中间件。
- 未登录访问管理接口时返回 HTTP `401`。
- 已登录但不具备管理员权限的普通用户访问时返回 HTTP `403`。
- 私有用户接口不再信任客户端传入或伪造的 `x-user-id` 请求头。
- 私有用户接口不再信任请求体中客户端传入或伪造的 `userId`。
- 用户身份统一从服务端验证通过的登录令牌中取得。

### `backend/auth.js`

- 增加 JWT 登录令牌的签发和验证逻辑。
- 从 `Authorization: Bearer <token>` 请求头识别真实登录用户。
- 根据服务器端环境变量 `ADMIN_USERNAMES` 判断管理员身份。
- 生产环境要求 `JWT_SECRET` 至少包含 32 个字符。
- JWT 密钥仅从服务器环境配置读取，不应写入源码或提交到代码仓库。

## 配套修改文件

### `admin/js/api.js`

- 管理后台请求自动携带 JWT。
- 令牌失效或认证失败时清除本地令牌。

### `admin/js/app.js`

- 调整管理后台登录流程。
- 登录成功后保存令牌。
- 完善退出登录及令牌清理逻辑。

### `backend/seedDevAdmin.js`

- 限制测试管理员账号只能在开发环境中创建。
- 防止生产环境意外生成测试管理员。

### `.env.example`

- 增加 `JWT_SECRET` 配置说明。
- 增加 `ADMIN_USERNAMES` 配置说明。
- 补充生产环境密钥安全要求。

## 验证结果

隔离测试及生产部署验证结果：

```text
UnauthenticatedAdmin=401
SpoofHeader=401
SpoofBody=401
```

上述结果表示：

- 未登录访问管理接口已被拒绝。
- 伪造 `x-user-id` 请求头不能冒充其他用户。
- 伪造请求体中的 `userId` 不能冒充其他用户。

## 修改前备份

修改前的相关文件备份位于：

```text
E:\bot\ailunwen-admin-auth-backup-20260804-163726
```

## 生产环境注意事项

- `JWT_SECRET` 应使用长度足够的强随机字符串，不得发送给无关人员。
- `ADMIN_USERNAMES` 必须仅包含经过授权的管理员用户名。
- 真实 `.env` 不应放入源码交付包或代码仓库。
- 修改认证逻辑后，应重新测试 HTTP `401`、`403`、管理员正常访问及伪造身份请求。
111111111111111111111111111111